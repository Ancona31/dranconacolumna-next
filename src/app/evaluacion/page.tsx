import Placeholder from "@/components/Placeholder";

export const metadata = { title: "Evaluación gratuita" };

export default function EvaluacionPage() {
  return <Placeholder title="Evaluación gratuita" phase={2} />;
}
